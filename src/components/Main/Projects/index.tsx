import ProjectsCarousel from "./ProjectsCarousel";

const Projects = () => {
  return (
    <section className="h-[100dvh]">
      <ProjectsCarousel />
    </section>
  );
};

export default Projects;
